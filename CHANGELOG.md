# Changelog

All notable changes to url-shelf are documented here.
The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [0.1.0] - Unreleased

### Added
- Project scaffold: Swift SPM executable target, `.app` bundle assembly,
  Developer ID signing / notarization wiring, Homebrew cask generation.
- RFP documents in `docs/en/` and `docs/ja/`.
- `NSStatusItem`-based menu bar skeleton that rebuilds its menu on open.
- `DisplayName` — filename to menu-label normalization (ordering prefix stripping).
